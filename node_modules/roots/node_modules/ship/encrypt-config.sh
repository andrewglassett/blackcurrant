#!/usr/bin/env sh -u

secret=`cat /dev/urandom | head -c 10000 | openssl sha1`
openssl aes-256-cbc -k "$secret" -in config.coffee -out config.coffee.enc -a
travis encrypt secret=$secret -r 'carrot/ship'
